# النسخة النهائية المنظمة لمشروع V-PUFT

هذه الحزمة مخصصة للإضافة إلى فرع:

`extension/trust-cache-byzantine-rsu`

بعد آخر تصحيح تحليلي عند:

`ef39427`

## المحتويات

- `results/final_paired_transport_crn/`: مخرجات التحليل المقترن Evidence-CRN النهائية.
- `docs/final_chapter5/`: الفصل الخامس بصيغ HTML وWord وPDF.
- `docs/final_chapter5/charts/`: الرسوم العلمية كصور مستقلة قابلة للنسخ.

## الحالة العلمية

الحكم النهائي:

`PASS WITH ANALYSIS-ONLY CORRECTIONS`

لا تحتاج النتائج إلى إعادة تشغيل SUMO أو إعادة حملة CRN.

## حدود إلزامية للاستخدام

- الدراسة `Post-hoc Supplementary/Exploratory` ولا يجوز وصفها بأنها Confirmatory.
- الأزمنة والفقد والطوابير مخرجات محاكاة ونمذجة، وليست قياسات حقلية.
- المقارنة الأساسية العادلة هي الموزع مقابل المركزي المتاح دائماً عند 0 ms.
- المركزي التشغيلي عند availability=0.995 هو محفز إتاحة نموذجي.
- توجد ثلاث حالات Topology × Density فريدة فقط بنقطة تعادل ضمن 0–200 ms.
- القيم الأخرى لنقاط التعادل استقراء نموذجي خارج مجال الفحص.
- لا يجوز تعميم عتبة v7 القديمة 167.80 ms على حملة الكثافة الجديدة.
- ملف الأوزان يصرح `selection_feasible=false`؛ الأوزان أفضل مرشح مرتب وليست حلاً مستوفياً لكل القيود المسبقة.

## الملفات التي يجب عدم حذفها

- `results/full_campaign_v7/`
- `results/full_campaign_v7/sensitivity_shared_views_mixedfix/selected_weights.json`
- `sumo/`
- `src/`
- `configs/`
- `scripts/`
- `tests/`

هذه الملفات لازمة للتوثيق أو إعادة الإنتاج. التنظيف يستهدف النتائج المؤقتة والسجلات القديمة فقط.

## ملاحظة عن ملف القرارات

حُفظت القرارات الخام باسم `paired_decisions.csv.gz` بدلاً من CSV غير مضغوط، حتى يبقى المستودع خفيفاً ولا يقترب الملف المفرد من حد GitHub. يمكن فك ضغطه عند الحاجة من دون أي فقد للبيانات.
